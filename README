Pure Python2 PXE (DHCP/TFTP) server.

Edit the head of server.py to change your preferred network settings.
Example TinyCore iso included along with relevant configs in netboot/

The DHCP/TFTP servers can also be used as part of other programs, as and library.

To run:
    sudo python2 server.py
